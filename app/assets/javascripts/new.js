// var drake = dragula(Array.from(document.getElementsByClassName('phase')));
//  drake.on('drop', (el, target) => {
//     this.props.moveCard(el.id, target.id);
//  });


//  console.log("custom js working or not")